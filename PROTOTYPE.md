# Racing Arcade 前台原型交互文档

> **文档版本**：v3.2
> **最后更新**：2026-06-16
> **覆盖范围**：Phase 1 MVP + Phase 2 社区与竞赛增强
> **关联文档**：PRD.md
> **变更记录**：
> - v3.0 — 赛事核心结构调整为 Competition → Round → Stage → Session；P5 统一为赛事详情页，P6 调整为分站详情页
> - v3.1 — MVP 阶段只支持 AC / ACC；服务器信息展示改为复用后台自动开服配置的可见字段
> - v3.2 — CompetitionRuleset 精简：赛制规则/积分规则/晋级规则合并到 description；Split 配置移至 Stage 层级；P5 概览 Tab 展示对应调整

---

## 目录

- [一、全局框架](#一全局框架)
- [二、页面清单与详细设计](#二页面清单与详细设计)
- [三、页面跳转关系图](#三页面跳转关系图)
- [四、核心交互流程](#四核心交互流程)
- [五、弹窗/浮层清单](#五弹窗浮层清单)
- [六、页面权限矩阵](#六页面权限矩阵)

---

# 一、全局框架

## 1.1 全局导航栏 (Top Navigation)

始终固定在页面顶部，包含以下元素：

| 元素 | 说明 |
|------|------|
| **Logo** | Racing Arcade 品牌 Logo，点击返回首页 |
| **导航菜单** | 赛事 / 日历 / 排行榜 / 新闻 |
| **区域切换器** | 下拉选择当前区域（CN / AP / AM / EU），切换后页面内容刷新为该区域数据 |
| **语言切换** | 中文 / English 切换按钮 |
| **通知铃铛** | 未读数量气泡，点击展开最近 20 条通知列表；"查看全部"跳转通知中心（仅登录用户可见） |
| **用户头像/登录** | 已登录：显示头像 + 下拉菜单（我的档案 / 我的赛事 / 我的车队 / 账号设置 / 退出登录）；未登录：显示"登录"按钮 |

## 1.2 全局状态与行为

| 全局行为 | 说明 |
|---------|------|
| **未登录用户** | 可浏览赛事列表/详情/排行榜/日历/新闻/公开档案；点击报名等需登录的操作时弹出登录弹窗 |
| **封禁用户** | 登录后全站顶部显示封禁横幅（含封禁原因和截止时间），禁止报名/抗议等操作，浏览不受限 |
| **信息补全拦截** | 注册后未补全必要信息的用户，全页遮罩层强制补全（常驻国家、语言偏好、主要游玩游戏） |
| **Token 过期** | 前端检测 401 响应，自动跳转登录页并提示"登录已过期" |

## 1.3 页脚 (Footer)

- 平台介绍 / 帮助中心链接 / 隐私政策 / 服务条款
- 社交媒体链接（Discord / Twitter / YouTube）
- MOZA Racing 品牌信息

---

# 二、页面清单与详细设计

## P1. 登录页 (`/login`)

**入口**：点击导航栏"登录"按钮、需登录的操作触发弹窗

### 页面元素

| 元素 | 说明 |
|------|------|
| 品牌Logo | Racing Arcade Logo |
| 标语 | 平台简介文案 |
| **Pit House SSO 登录按钮** | 主按钮，点击跳转 Pit House OAuth 授权页 |
| **Discord 登录按钮** | 辅助按钮，点击跳转 Discord OAuth 页 |
| **Steam 登录按钮** | 辅助按钮，点击跳转 Steam OpenID 页 |
| 区域/语言信息 | 当前区域和语言提示 |

### 交互逻辑

1. 用户选择一种登录方式 → 跳转对应第三方授权页
2. 授权成功回调本站：
   - **已绑定账号** → 直接登录，跳转首页
   - **新账号** → 跳转注册补全页 (P2)
3. 授权被拒绝 → 返回登录页，无错误提示
4. 第三方服务不可用 → 显示错误提示"登录服务暂时不可用，请稍后再试"

---

## P2. 注册信息补全页 (`/register/complete`)

**入口**：首次第三方登录成功后自动跳转；或未补全信息的用户登录后强制跳转（不可跳过的遮罩层）

### 页面元素

| 元素 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 昵称 | 文本输入 | 是 | 平台显示名 |
| 头像上传 | 图片上传 | 否 | 默认使用第三方头像 |
| **常驻国家/区域** | 下拉选择 | 是 | 国家列表，选择后自动映射到 CN/AP/AM/EU 区域 |
| **语言偏好** | 单选 | 是 | 中文 / English |
| **主要游玩游戏** | 多选标签 | 是 | AC / ACC |
| 完成注册按钮 | 按钮 | — | 提交后进入首页 |

### 交互逻辑

1. 所有必填字段校验通过后"完成注册"按钮可点击
2. 提交成功 → 跳转首页
3. 未补全信息关闭页面 → 下次登录强制显示遮罩层，不可跳过

---

## P3. 首页 (`/`)

### 页面元素

| 区域 | 说明 |
|------|------|
| **Hero Banner** | 大图轮播区，展示精选 Competition 宣传图，可点击跳转赛事详情 |
| **近期赛事** | 展示 4-6 张 Competition 卡片，仅含存在可报名 Round 或进行中 Stage 的赛事，按最近可行动时间排序 |
| **正在进行** | 当前进行中的赛事（如有），标注 LIVE 标识 |
| **新闻公告** | 最近 3-5 条新闻，显示标题/摘要/时间/分类标签 |
| **排行榜速览** | 总积分 TOP 5 车手列表 |
| **合作伙伴/MOZA品牌** | MOZA 品牌信息和设备展示 |

### Competition 卡片元素

- 封面图 + 游戏平台标签
- 赛事名称
- 车型组
- 当前/下一 Round 名称、赛道、比赛时间
- 当前 Stage 标签
- 报名人数（X / Y）+ 报名状态标签（右对齐）

### 交互逻辑

- 点击 Competition 卡片 → 跳转赛事详情页 (P5)
- 点击卡片中的 Round 信息 → 跳转分站详情页 (P6)
- 点击新闻条目 → 跳转新闻详情页 (P20)
- 点击排行榜车手 → 跳转车手档案页 (P12)
- 点击"查看全部" → 跳转赛事列表页 (P4)

---

## P4. 赛事列表页 (`/events`)

### 页面结构（自上而下）

| 区域 | 说明 |
|------|------|
| **页面标题** | "赛事" |
| **报名中** | 当前存在可报名 Round 的 Competition，按最近可报名 Round 的报名开放时间由新到旧排序 |
| **进行中** | 当前存在进行中 Stage/Session 的 Competition，例如预选赛服务器开放中 |
| **已结束赛事** | 所有 Round 均已结束的 Competition，按最近结束 Round 时间由新到旧排序 |

### Competition 卡片元素

- 封面图 + 游戏平台标签
- 赛事名称 + 车型组
- Round 数量（仅多 Round 赛事显示）
- 当前/下一 Round：名称、赛道、时间
- 当前 Stage 标签：预选赛开放中 / 正赛日 / 决赛 / 报名中 / 已结束
- 报名人数 / 容量 + 报名状态标签（右对齐）
- 预选赛开放中时，额外显示晋级线摘要（如"前 30 晋级"）

### 交互逻辑

- 点击 Competition 卡片 → 跳转赛事详情页 (P5)
- 点击卡片中的当前/下一 Round 信息 → 跳转分站详情页 (P6)
- 切换区域 → 刷新列表展示该区域赛事

---

## P5. 赛事详情页 (`/events/{competitionId}`)

统一用于所有赛事，无论单场赛还是多站锦标赛。页面主对象是 Competition，Round / Stage / Session 作为内部层级展示。

### 页面结构（自上而下）

| 区域 | 元素 | 说明 |
|------|------|------|
| **取消横幅** | 红色横幅"本赛事已取消"+取消原因 | Competition 或当前 Round 已取消时显示 |
| **封面区** | 封面大图 + Competition 名称 + 状态标签 | — |
| **基础信息栏** | 游戏平台、车型组、Round 数量、当前 Stage、下一比赛时间（双时区） | 不直接显示所属区域；时间同时显示赛事当地时区和用户本地时区 |
| **Tab 切换** | 概览 / 赛程 / 预选榜 / 成绩 / 积分榜 | 单 Round 赛事可弱化 Tab 数量，但结构保持一致 |
| **概览 Tab** | 描述（含赛制规则、积分规则、晋级规则）、准入条件、积分表 | Competition 级公共信息 |
| **赛程 Tab** | Round 列表 + 每个 Round 的 Stage 时间线 | 当前/下一 Round 置顶 |
| **预选榜 Tab** | 选择 Round/Stage 后展示圈速榜、晋级线、候选名单 | 仅有 qualifier Stage 时显示；否则隐藏 |
| **成绩 Tab** | 按 Round / Stage / Session 筛选成绩 | 成绩绑定 Session |
| **积分榜 Tab** | Competition 总积分、分 Round 积分、晋级状态 | — |
| **资源下载** | 富文本渲染的资源信息（下载链接、安装说明等） | 公开可见，无需登录；仅在有资源内容时显示 |
| **右侧栏·当前行动** | 当前/下一 Round、当前 Stage、报名状态、报名按钮、添加到日历 | sticky 固定 |
| **右侧栏·服务器信息** | 根据当前可参与 Session 展示 AC / ACC 自动开服配置中的可见字段：服务器名称、密码、直连链接/加入指引、赛道、车辆、规则摘要 | 仅报名后可见；不展示端口、管理员密码、Steam ID/GUID、插件地址 |
| **赛事公告** | 关联的公告列表 | — |

### 报名按钮交互状态

| 用户状态 | 按钮展示 | 点击行为 |
|---------|---------|---------|
| 未登录 | "登录后报名" | 弹出登录弹窗 |
| 已登录-未报名-名额充足 | "立即报名" | 进入报名流程 |
| 已登录-未报名-名额已满 | "名额已满-加入候补" | 加入候补名单 |
| 已登录-已报名 | "已报名 ✓"+取消报名按钮 | 显示报名详情和取消选项 |
| 已登录-被禁赛 | "当前无法报名"（灰色） | 提示查看账号状态 |
| **Round 尚未开放报名** | "尚未开放报名"（灰色文字） | Upcoming 状态，不显示报名按钮 |
| **Upcoming 状态重要时间** | 仅显示比赛时间，不显示报名截止时间 | — |
| 报名已截止 | "报名已截止"（灰色） | 不可点击 |

### 报名流程

1. 点击"立即报名"
2. 检查 Steam 绑定状态 → 缺少则弹出绑定引导
3. 检查准入条件和赛制规则 → 若有：弹出"报名前须知"弹窗（准入条件在上，赛制规则在下）+ 勾选框
4. 确认报名 → 成功：显示报名成功+添加日历按钮+分组/晋级待公布提示
5. 跨区域赛事：额外提示"该赛事的服务器可能不位于您的当前区域，服务器连通性和延时可能受影响。"

### 取消报名流程

1. 点击"取消报名"
2. 检查是否在取消报名截止时间前 → 已过期：提示联系管理员
3. 弹出确认对话框 → 确认 → 名额释放

### 其他交互

- **添加到日历**：点击按钮 → 下载 .ics 文件（包含 Competition / Round / Session 名称、时间、赛道、服务器信息）
- **Round 跳转**：点击 Round 卡片 → 跳转分站详情页 (P6)，并定位当前 Stage
- **预选榜查看**：点击"查看预选榜" → 切换到预选榜 Tab，并选中对应 Round
- **成绩展示**：成绩表格含名次、车手（可点击→档案页）、车队（可点击→车队页）、完赛时间、最快圈、差距、状态（完赛/DNF/DSQ等）、积分
- **多Split赛事**：Session 内切换各 Split 成绩
- **提交抗议**：赛后一定时间内，成绩区显示"提交抗议"按钮 → 跳转抗议提交页 (P13)

---

## P6. 分站详情页 (`/events/{competitionId}/rounds/{roundId}`)

分站详情页展示某个 Round 内部的 Stage / Session 流程，适合含预选赛、正赛日、决赛等复杂流程的赛事。

### 页面结构（自上而下）

| 区域 | 说明 |
|------|------|
| **页面布局** | 左侧 Stage/Session 主内容 + 右侧固定行动栏 | 侧边栏 sticky 固定 |
| **Round 头部** | Competition 名称、Round 名称、赛道/布局、Round 状态、返回赛事详情 | — |
| **Stage 时间线** | 横向或纵向展示预选赛、正赛日、决赛等 Stage | 当前 Stage 高亮 |
| **当前 Stage 卡片** | Stage 名称、时间窗口、规则、当前榜单/结果、当前可进入 Session | 默认展开 |
| **Session 列表** | 每个 Stage 下展示平台 Session：practice、qualifying、race、timeTrial | 可折叠 |
| **右侧栏·报名操作** | Round 报名状态、报名/取消报名、报名人数/容量、添加到日历 | 报名目标默认是 Round |
| **右侧栏·当前服务器** | 当前可参与 Session 的服务器名称、密码、直连链接/加入指引、赛道、车辆、规则摘要 | 仅报名且 Session 可见时显示；字段来自 AC / ACC 开服配置可见子集 |
| **直播/回放** | 当前 Session 直播，历史 Session 回放 | 有链接时显示 |
| **成绩与抗议** | 按 Session 展示成绩，赛后可提交抗议 | 成绩发布后显示 |

### 预选赛 Stage 展示规则

- Stage 类型为 qualifier，默认展示在 Round 详情页最上方
- 通常使用长时间开放的 practice Session 承载预选赛，开放时长由后台配置
- 平台中的一个 practice Session 可对应游戏服务器里的多个实际 Session；后台可设置固定重启间隔，直到预选赛 Stage 结束
- 预选赛 Stage 结束后，系统合并所有游戏内 Session 成绩，默认每位车手取最快一次有效圈速
- 展示榜单字段：排名、车手、车队、最佳圈速、有效圈数、最后刷新时间
- 展示晋级线：晋级人数、当前最后晋级名次、差距
- 晋级名单在 Stage 结束后生成，管理员复核后发布
- 已晋级车手在正赛日 Stage 中显示"已入围"状态；未晋级车手可显示"未入围/候补"

### 正赛日 Stage 展示规则

- Stage 类型为 race_day
- Session 顺序通常为 Practice → Qualifying → Race
- Practice/Qualifying/Race 分别展示开始时间、时长、服务器信息
- Qualifying 成绩可决定 Race 发车顺位
- Race 成绩产生 Round 积分和抗议入口

### AC / ACC 服务器信息展示规则

- MVP 阶段只展示 AC / ACC 自建服务器信息
- 服务器名称、密码、赛道、车辆、Session 时长、规则摘要优先来自后台自动开服配置
- 若后台尚未生成配置，服务器卡片仍保留，提示"比赛进入方式稍后提供"
- 已报名车手可见加入密码和直连链接；未报名用户不可见服务器卡片
- 不展示端口、管理员密码、Steam ID/GUID、插件地址、容器路径、结果文件路径等后台字段
- 长时间开放 practice 预选的服务器可显示"后台将按固定间隔自动重启，榜单按最快有效圈速合并"

### 决赛 Stage 展示规则

- Stage 类型为 final
- 仅对满足晋级条件的车手展示参赛资格
- 可包含一个或多个 race Session
- 决赛成绩可影响 Competition 总积分、奖项或总冠军

### 交互逻辑

- 点击 Stage 时间线节点 → 滚动到对应 Stage
- 点击 Session 服务器链接 → 打开游戏直连或外部 Session 页面
- 点击"查看完整榜单" → 展开对应 Stage 榜单
- 点击成绩中的车手 → 跳转车手档案页 (P12)
- 点击成绩中的车队 → 跳转车队公开页 (P18)
- 点击"提交抗议" → 跳转抗议提交页 (P13)，带入 Session ID

---

## P7. 赛事日历页 (`/calendar`)

### 页面元素

| 区域 | 说明 |
|------|------|
| **视图切换** | 月历 / 周历 / 列表 三个Tab |
| **筛选栏** | AC / ACC、仅已报名赛事 |
| **日历区域** | 根据视图模式展示 |

### 月历视图

- 按月展示，日期格子内显示赛事卡片（缩略信息：名称+时间+游戏平台标签颜色）
- 同一天多场赛事 → 格子内折叠，点击展开
- 月份切换：上个月/下个月箭头

### 周历视图

- 按周展示，每天按时间线纵向排列赛事
- 事件以时间块形式展示

### 列表视图

- 按时间顺序排列，每行显示完整赛事信息

### 日历筛选

| 筛选项 | 说明 |
|--------|------|
| 游戏平台 | AC / ACC 多选 |
| 已报名赛事 | 仅展示用户已报名的赛事（需登录） |

### 日历卡片元素

- Competition 名称、Round 名称、Stage/Session 时间、游戏平台标签（颜色区分）、报名状态指示

### 交互逻辑

- 点击 Competition 卡片 → 跳转赛事详情页 (P5)
- 点击 Round/Session 卡片 → 跳转分站详情页 (P6)，自动定位对应 Stage/Session
- 切换筛选条件 → 日历刷新
- 勾选"仅已报名" → 仅显示用户已报名赛事（需登录）

---

## P8. 排行榜页 (`/leaderboard`)

### 页面元素

| 区域 | 说明 |
|------|------|
| 页面标题 | "排行榜" |
| Tab切换 | 总积分排行 / 胜场排行 / 参赛次数排行 / 领奖台排行 |
| 筛选栏 | 时间段（本赛季/全部）、AC / ACC |
| 排行表格 | 排名、车手头像+昵称、对应数据值 |

### 交互逻辑

- 切换Tab → 刷新排行数据
- 切换筛选 → 刷新排行数据
- 切换网站区域 → 刷新排行数据（仅统计当前区域赛事的成绩和积分，同一车手在不同区域排名可能不同）
- 点击车手 → 跳转车手档案页 (P12)
- 分页加载

---

## P9. 通知中心页 (`/notifications`)

**入口**：导航栏通知铃铛 → "查看全部"

### 页面元素

| 区域 | 说明 |
|------|------|
| 页面标题 | "通知中心" |
| 操作栏 | "全部标记已读"按钮 |
| 通知列表 | 分页展示所有通知 |

### 单条通知元素

- 通知类型图标
- 通知标题
- 通知摘要内容
- 时间
- 已读/未读状态（未读加粗显示）

### 交互逻辑

- 点击通知 → 跳转至相关页面（赛事详情/分站详情/抗议详情等）+ 标记已读
- "全部标记已读" → 批量标记
- 筛选 → 按类型过滤
- 分页加载

---

## P10. 账号设置页 (`/settings`)

**入口**：导航栏用户下拉菜单 → "账号设置"

### Tab 1: 个人资料

| 元素 | 类型 | 说明 |
|------|------|------|
| 头像 | 图片上传 | — |
| 昵称 | 文本输入 | 唯一性校验 |
| 个人简介 | 文本域 | — |
| 常驻国家/区域 | 下拉选择 | 修改后影响默认区域 |
| 语言偏好 | 单选 | 中文/English |

### Tab 2: 游戏信息

| 元素 | 类型 | 说明 |
|------|------|------|
| Steam ID | 展示 | 通过 Steam 绑定自动获取，可设置公开/私密 |
| Steam 绑定状态 | 展示+绑定/解绑按钮 | — |
| 主要游玩游戏 | 多选标签 | — |

### Tab 3: MOZA设备

| 元素 | 类型 | 说明 |
|------|------|------|
| 已拥有设备列表 | 展示（从 Pit House 自动同步） | 不可手动添加，仅展示 Pit House 后台已注册/激活的设备 |
| 设备展示勾选 | 多选勾选 | 从已拥有设备中选择要在档案页公开展示的设备 |
| 设备展示开关 | 开关 | 整体公开/私密，默认公开 |

### Tab 4: 第三方账号

| 元素 | 说明 |
|------|------|
| Pit House | 已绑定（主账号，不可解绑） |
| Discord | 绑定/解绑按钮 + 绑定状态 |
| Steam | 绑定/解绑按钮 + 绑定状态 |

### Tab 5: 通知偏好

| 元素 | 说明 |
|------|------|
| Pit House 推送总开关 | 开/关（默认开） |
| 邮件通知总开关 | 开/关 |
| 各类通知的渠道开关 | 报名确认、赛事取消、成绩公布、抗议通知等逐项配置 Pit House / 邮件渠道 |

### 交互逻辑

- 修改即时保存或点击"保存"按钮
- 绑定Discord/Steam → 跳转授权页 → 回调后显示绑定状态
- 解绑唯一登录方式 → 按钮置灰，提示"不能解绑唯一的登录方式"

---

## P11. 车手档案页 (`/driver/{id}`)

**入口**：点击任意车手昵称/头像

### 页面元素

| 区域 | 说明 |
|------|------|
| 头像+昵称+所在国家 | — |
| 个人简介 | — |
| MOZA设备展示 | 设备图标+名称列表（若用户设为公开） |
| **统计数据** | 总参赛次数、胜场数、领奖台数、总积分 |
| **参赛历史** | 赛事列表：赛事名称（可点击）、名次、得分、日期 |
| **所属车队** | 车队名称（可点击→车队页） |

### 可见性控制

- Steam ID 字段：用户设为私密的，其他用户看不到
- MOZA设备：用户设为私密的，其他用户看不到
- 基础信息和统计数据：始终公开
- 自己查看自己的档案页时，额外显示"编辑档案"按钮

### 交互逻辑

- 点击参赛历史中的 Competition → 跳转赛事详情页 (P5)
- 点击参赛历史中的 Round/Session → 跳转分站详情页 (P6)
- 点击车队 → 跳转车队公开页 (P17)
- 点击"编辑档案" → 跳转账号设置页 (P10)

---

## P12. 我的赛事页 (`/my-events`)

**入口**：导航栏用户下拉菜单 → "我的赛事"

### 页面元素

| 区域 | 说明 |
|------|------|
| Tab切换 | 即将到来 / 正在进行 / 已完成 |

### 列表结构

列表按 Competition 分组展示用户已报名的 Round。

#### Competition 分组卡片

**折叠状态**显示：
- Competition 名称
- 游戏平台标签
- 车型组
- 已报名 Round 数量
- 最近一个 Round 的当前 Stage、时间和报名状态
- 展开/折叠箭头

**展开后**显示该 Competition 下匹配当前 tab 状态的 Round 列表，每个 Round 展示：
- Round 名称（如"第 12 站 - 铃鹿"）
- 赛道 / 布局
- 当前 Stage（预选赛 / 正赛日 / 决赛）
- 下一 Session 时间
- 报名状态 / Split 编号 / 晋级状态（如有）
- "查看详情"按钮 → 跳转分站详情页 (P6)

### 交互逻辑

- Competition 卡片点击展开/折叠 → 显示/隐藏已报名 Round 列表
- Competition 名称点击 → 跳转赛事详情页 (P5)
- Round 的"查看详情" → 跳转分站详情页 (P6)
- Tab 切换过滤不同状态 Round
- 同一 Competition 下不同状态的 Round 分别出现在对应 Tab 中

---

## P13. 抗议提交页 (`/events/{competitionId}/rounds/{roundId}/sessions/{sessionId}/protest/new`)

**入口**：赛事详情页成绩 Tab 或分站详情页 Session 成绩区 → "提交抗议"按钮（仅赛后规定时间内可见）

### 页面元素

| 元素 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 抗议对象 | 搜索选择 | 是 | 搜索并选择被抗议的车手 |
| 抗议类型 | 单选 | 是 | 危险驾驶 / 多次变线 / 其他 |
| 详细描述 | 文本域 | 是 | — |
| 证据上传 | 文件上传/URL输入 | 否 | 截图/视频链接 |
| 发生圈数 | 数字输入 | 否 | — |
| 赛道位置 | 文本输入 | 否 | 如"T1弯道" |
| 提交按钮 | — | — | — |

### 交互逻辑

1. 填写完整信息 → 点击提交
2. 系统校验抗议窗口是否已关闭 → 已关闭：提示"抗议窗口已关闭"
3. 提交成功 → 显示抗议编号，跳转我的抗议列表 (P14)
4. 被抗议车手收到站内+邮件通知

---

## P14. 我的抗议列表页 (`/my-protests`)

**入口**：用户下拉菜单或通知跳转

### 页面元素

| 区域 | 说明 |
|------|------|
| Tab切换 | 我提交的 / 我收到的 |
| 抗议列表 | 抗议编号、关联赛事（可点击）、抗议类型、状态标签、提交时间 |

### 状态标签

待审核 / 审核中 / 已裁决 / 已驳回

### 交互逻辑

- 点击抗议 → 展开抗议详情（描述、证据、裁决结果）
- 已裁决的抗议显示裁决理由和处罚信息
- 被处罚的车手可点击"提出申诉"（24小时内）→ 弹出申诉提交弹窗
- 点击关联赛事 → 跳转赛事详情页 (P5)
- 点击关联 Round/Session → 跳转分站详情页 (P6) 并定位对应 Session

---

## P15. 申诉提交弹窗

**入口**：裁决通知详情中"提出申诉"按钮

### 页面元素

- 申诉理由（文本域，必填）
- 补充证据（文件上传/URL，可选）
- 提交按钮

### 交互逻辑

- 提交后申诉进入复审状态
- 申诉结果通知双方

---

## P16. 车队管理页 (`/my-team`)

**入口**：导航栏用户下拉菜单 → "我的车队"

### 未加入车队时

- "创建车队"按钮
- 或通过邀请链接加入

### 已加入车队时

| 区域 | 说明 |
|------|------|
| 车队信息 | Logo、名称、简介（队长可编辑） |
| 成员列表 | 头像+昵称+角色（队长/成员）+加入时间 |
| 邀请成员 | 搜索用户+发送邀请按钮（仅队长可见） |
| 操作按钮 | 移除成员（队长）/ 转让队长（队长）/ 退出车队（成员）/ 解散车队（队长） |

### 创建车队弹窗/页面

- 车队名称（唯一性校验）
- Logo上传
- 车队简介
- 提交按钮

### 交互逻辑

- 邀请成员 → 被邀请者收到通知 → 接受/拒绝/超时过期
- 移除成员 → 确认对话框（提示是否影响进行中赛事）→ 通知被移除者
- 退出车队 → 确认对话框 → 通知队长
- 解散车队 → 确认对话框（有进行中赛事时禁止）→ 通知所有成员
- 转让队长 → 确认对话框 → 通知新队长

---

## P17. 车队公开页 (`/team/{id}`)

**入口**：点击车队名称/Logo

### 页面元素

| 区域 | 说明 |
|------|------|
| 车队Logo+名称+简介 | — |
| 成员列表 | 头像+昵称+角色（队长/成员），可点击→车手档案 |
| 参赛历史 | 车队参加过的 Competition / Round / Session 列表 |
| 成绩统计 | 总参赛次数、最佳名次、总积分等 |

### 交互逻辑

- 点击成员 → 跳转车手档案页 (P11)
- 点击参赛的 Competition → 跳转赛事详情页 (P5)
- 点击参赛的 Round/Session → 跳转分站详情页 (P6)

---

## P18. 新闻列表页 (`/news`)

### 页面元素

| 区域 | 说明 |
|------|------|
| 页面标题 | "新闻与公告" |
| 新闻卡片列表 | 按发布时间倒序 |

### 卡片元素

- 封面图
- 标题
- 分类标签
- 发布时间
- 摘要

### 交互逻辑

- 点击卡片 → 跳转新闻详情页 (P19)
- 筛选 → 刷新列表

---

## P19. 新闻详情页 (`/news/{id}`)

### 页面元素

| 区域 | 说明 |
|------|------|
| 标题 | — |
| 分类标签+发布时间+作者 | — |
| 封面图 | — |
| 正文内容 | 富文本渲染 |
| 相关赛事链接 | 如赛事公告关联赛事 |

### 交互逻辑

- 点击相关 Competition → 跳转赛事详情页 (P5)
- 点击相关 Round/Session → 跳转分站详情页 (P6)

---

# 三、页面跳转关系图

```
首页 (P3)
 ├──→ 赛事列表页 (P4)
 │     ├──→ 赛事详情页 (P5) [点击 Competition 卡片]
 │     └──→ 分站详情页 (P6) [点击 Round 信息]
 ├──→ 赛事日历页 (P7)
 │     ├──→ 赛事详情页 (P5) [点击 Competition]
 │     └──→ 分站详情页 (P6) [点击 Round/Session]
 ├──→ 排行榜页 (P8) ──→ 车手档案页 (P11)
 ├──→ 新闻列表页 (P18) ──→ 新闻详情页 (P19)
 └──→ 登录页 (P1) ──→ 注册补全页 (P2)

赛事详情页 (P5)
 ├──→ 车手档案页 (P11) [点击参赛车手]
 ├──→ 车队公开页 (P17) [点击车队]
 ├──→ 分站详情页 (P6) [点击 Round]
 ├──→ 抗议提交页 (P13) [赛后提交抗议]
 └──→ .ics日历文件下载 [添加到日历]

分站详情页 (P6)
 ├──→ 车手档案页 (P11) [点击榜单/成绩车手]
 ├──→ 车队公开页 (P17) [点击成绩车队]
 ├──→ 抗议提交页 (P13) [Session 成绩中提交抗议]
 └→ 外部资源下载链接 [点击资源下载]

用户菜单（导航栏）
 ├──→ 车手档案页 (P11) [我的档案]
 ├──→ 我的赛事页 (P12)
 │     ├──→ 赛事详情页 (P5)
 │     └──→ 分站详情页 (P6)
 ├──→ 车队管理页 (P16) ──→ 车队公开页 (P17)
 ├──→ 我的抗议列表页 (P14) ──→ 申诉提交 (P15)
 ├──→ 账号设置页 (P10)
 └──→ 退出登录 → 首页 (P3)

通知中心 (P9)
 ├──→ 赛事详情页 (P5) [Competition 相关通知]
 ├──→ 分站详情页 (P6) [Round/Session 相关通知]
 ├──→ 我的抗议列表页 (P14) [抗议通知]
 ├──→ 车队管理页 (P16) [车队邀请]
 └──→ 车手档案页 (P11) [其他]
```

---

# 四、核心交互流程

## 4.1 用户登录 → 浏览 → 报名完整流程

```
登录页(P1) → 第三方授权 → 注册补全(P2)[首次] → 首页(P3)
  → 赛事列表(P4)/日历(P7)

  [统一赛事路径]
  → 赛事详情(P5) → 选择当前/下一 Round → 分站详情(P6)
  → 报名 Round → 参与当前 Stage/Session
  → [预选赛] 查看圈速榜/晋级线 → [入围] 进入正赛日
  → 查看 Session 成绩 → [异议?] → 提交抗议(P13)
```

## 4.2 报名通用流程（Competition / Round 通用）

```
点击"立即报名"
  → [缺少 Steam 绑定?] → 弹窗引导绑定 Steam
  → [有准入条件/赛制规则?] → 弹出"报名前须知"弹窗 → 勾选确认
  → 确认报名 → 成功：报名成功+添加日历+分组/晋级待公布
  → 跨区域赛事：提示"该赛事的服务器可能不位于您的当前区域，服务器连通性和延时可能受影响。"
```

## 4.3 车队创建 → 邀请 → 报名流程

```
我的车队(P16) → 创建车队 → 填写信息 → 创建成功
  → 邀请成员 → 搜索用户 → 发送邀请
  → 成员接受邀请通知 → 加入车队
  → 赛事详情(P5)/分站详情(P6) → 队长报名 Round → 勾选参赛成员 → 报名成功
```

## 4.4 抗议 → 裁决 → 申诉流程

```
赛事详情(P5)成绩 Tab / 分站详情(P6)Session 成绩区
  → 提交抗议(P13) → 填写抗议信息 → 提交
  → 管理员裁决 → 双方收到通知
    → [被处罚方不服?] → 提出申诉(P15) → 填写申诉理由
    → 管理员复审 → 最终裁决通知
```

## 4.5 候补转正流程

```
赛事详情(P5)/分站详情(P6) → 点击"名额已满-加入候补" → 进入候补名单
  → 等待有人取消报名 → 收到候补转正通知
  → 限定时间内确认参赛 → 报名成功
  → 超时未确认 → 名额顺延下一位
```

## 4.6 账号绑定/解绑流程

```
账号设置(P10) → 第三方账号Tab
  → 绑定Discord/Steam → 跳转授权页 → 回调绑定
  → 解绑 → [仅剩一种?] → 禁止，提示先绑定其他
           → [多种方式?] → 确认解绑 → 成功
```

---

# 五、弹窗/浮层清单

| 弹窗 | 触发场景 | 内容 |
|------|---------|------|
| **登录弹窗** | 未登录用户点击需登录操作 | 同登录页内容（Pit House/Discord/Steam按钮） |
| **Steam绑定补充弹窗** | 报名时缺少 Steam 绑定 | Steam 绑定引导 |
| **报名前须知弹窗** | 报名有准入条件或赛制规则的赛事 | 标题"报名前须知" + 准入条件（图标+内容） + 赛制规则（图标+内容） + 勾选框 + 确认按钮 |
| **报名成功弹窗** | 报名成功后 | 成功提示 + 添加日历按钮 + 分组/晋级待公布提示 |
| **取消报名确认弹窗** | 点击取消报名 | 确认对话框 |
| **跨区域提示弹窗** | 报名非本区域赛事 | "该赛事的服务器可能不位于您的当前区域，服务器连通性和延时可能受影响。" |
| **创建车队弹窗** | 点击创建车队 | 车队名称/Logo/简介/区域表单 |
| **邀请成员弹窗** | 队长点击邀请 | 搜索用户输入框 + 发送按钮 |
| **移除成员确认弹窗** | 队长移除成员 | 确认对话框（提示是否影响进行中赛事） |
| **解散车队确认弹窗** | 队长点击解散 | 确认对话框（有进行中赛事时禁止） |
| **申诉提交弹窗** | 被处罚方点击提出申诉 | 申诉理由 + 补充证据 + 提交按钮 |
| **封禁提示横幅** | 被封禁用户登录 | 封禁原因 + 截止时间（全页顶部横幅） |
| **信息补全遮罩** | 未补全信息用户登录 | 全屏不可跳过，强制填写（常驻国家/语言/游戏） |

---

# 六、页面权限矩阵

| 页面 | 路由 | 访客 | 车手 | 备注 |
|------|------|------|------|------|
| 首页 | `/` | 可浏览 | 可浏览 | — |
| 登录页 | `/login` | 可访问 | — | 已登录不显示 |
| 注册补全 | `/register/complete` | — | 首次注册 | 强制补全 |
| 赛事列表 | `/events` | 可浏览 | 可浏览 | — |
| 赛事详情 | `/events/{competitionId}` | 可浏览+资源下载 | 可浏览+报名+资源下载 | 统一 Competition 详情页；服务器信息仅已报名可见 |
| 分站详情 | `/events/{competitionId}/rounds/{roundId}` | 可浏览 | 可浏览+报名+服务器信息 | 展示 Round 内 Stage / Session 流程 |
| 赛事日历 | `/calendar` | 可浏览 | 可浏览+导出ics | — |
| 排行榜 | `/leaderboard` | 可浏览 | 可浏览 | — |
| 通知中心 | `/notifications` | 不可访问 | 仅自己 | — |
| 账号设置 | `/settings` | 不可访问 | 仅自己 | — |
| 车手档案 | `/driver/{id}` | 可浏览公开信息 | 可浏览+编辑自己 | 私密字段不展示给他人 |
| 我的赛事 | `/my-events` | 不可访问 | 仅自己 | — |
| 抗议提交 | `/events/{competitionId}/rounds/{roundId}/sessions/{sessionId}/protest/new` | 不可访问 | 仅参赛车手 | 抗议绑定具体 Session |
| 我的抗议 | `/my-protests` | 不可访问 | 仅自己 | — |
| 车队管理 | `/my-team` | 不可访问 | 仅车队成员 | — |
| 车队公开页 | `/team/{id}` | 可浏览 | 可浏览 | — |
| 新闻列表 | `/news` | 可浏览 | 可浏览 | — |
| 新闻详情 | `/news/{id}` | 可浏览 | 可浏览 | — |

---

> **文档结束**
> 本原型交互文档覆盖 Phase 1 + Phase 2 共 19 个页面和 13 个弹窗/浮层，后续随需求变更持续更新。
>
> **v2.0 变更摘要**：
> - **重大重构**：锦标赛子赛事取消独立详情页，统一在锦标赛详情页 (P6) 内以"下一场可报名"/"未来赛事"/"过往赛事"三区域展示
> - 赛事详情页拆分为两种：独立赛事详情页 (P5) 和锦标赛详情页 (P6)
> - 赛事列表 (P4) 区分独立赛事卡片和锦标赛卡片两种样式
> - 赛事列表 (P4) 筛选条件精简为：游戏平台、车型组、时间范围（共 3 项）
> - 日历页 (P7) 筛选移除"赛事类型"选项，保留：游戏平台、已报名赛事
> - 排行榜页 (P8) 筛选补充游戏平台选项，现为：时间段、游戏平台、区域
> - 新闻页 (P18) 筛选补充区域选项，现为：分类、区域
> - 通知中心 (P9) 移除筛选功能
> - 全站游戏平台名称统一为 AC / ACC / AC Evo / iRacing / LMU / rF2 / ETS2（移除"PC"后缀，新增 rF2、ETS2，移除 AMS2）
> - 车型组精简为 GT3 / GT4 / Formula
> - 赛事详情页 (P5) 和锦标赛详情页 (P6) 不再直接显示所属区域
> - 锦标赛详情页 (P6) 改为左侧主内容（含"锦标赛信息"/"赛程"Tab 切换）+ 右侧固定侧边栏布局
> - 锦标赛详情页 (P6) 下一场赛事信息及报名操作移至右侧栏，未来赛事和过往赛事移至"赛程"Tab
> - 锦标赛详情页 (P6) 所有已报名的子赛事均显示服务器信息
> - 全站所有跳转逻辑区分独立赛事→P5、锦标赛子赛事→P6
> - 锦标赛详情页 (P6) 新增"成绩"Tab，统一展示各历史赛事成绩，支持按赛事和环节（正赛/排位赛）筛选
> - 锦标赛详情页 (P6) 成绩 Tab：必须选择具体赛事（无"全部赛事"选项），成绩表新增"完成圈数"列
> - 锦标赛详情页 (P6) 成绩 Tab 切换时右侧栏隐藏，成绩区域全宽展示
> - 锦标赛详情页 (P6) 赛程中未来赛事/过往赛事不显示状态标签
> - 锦标赛详情页 (P6) 过往赛事"查看成绩"改为跳转成绩 Tab 并自动筛选
> - 锦标赛详情页 (P6) 侧边栏报名人数数字独立一行凸显，状态标签右对齐，已报名/赛事状态不共存
> - 锦标赛详情页 (P6) 服务器信息独立为卡片（绿色边框高亮），位于主卡片下方
> - 锦标赛详情页 (P6) 封面区和赛事名称旁移除状态标签
> - 全站移除区域信息展示：赛事卡片、排行榜、新闻页、车手页、车队页均不显示区域
> - 赛事列表 (P4) 赛事卡片和锦标赛卡片移除区域标签
> - 排行榜页 (P8) 移除区域筛选和区域列
> - 新闻页 (P18) 移除区域筛选
> - 车手档案页 (P11) 移除区域显示，仅保留国家
> - 车队管理页 (P16) 和车队公开页 (P17) 移除区域显示
> - 日历页 (P7) 移除区域筛选
> - 新增赛事状态 Upcoming（未来），新增 registrationOpenAt 字段，管理员可设置报名起始时间
> - **赛事状态动态计算**：新增 `getEventStatus(event)` 工具函数，根据真实时间自动计算赛事状态（仅 Cancelled/Draft 为管理员手动设置，其余全部由函数派生），修正了 mock 数据中 status 与日期不一致的问题
> - **积分规则增强**：支持自定义文字 + 结构化积分表格（`ScoringTableEntry`：名次/积分/双语备注），备注为空时不显示备注列；两者可同时存在，先文字后表格；新增 `ScoringRulesCard` 共享组件
> - 锦标赛侧边栏同时展示 RegistrationOpen 和 RegistrationClosed 状态的赛事（即下一场未开赛的赛事）
> - 锦标赛详情页 (P6) 赛程中未来赛事不显示状态标签、报名截止时间和报名按钮
> - 锦标赛详情页 (P6) 历史赛事不显示服务器信息，回放按钮改为"直播回放"
> - 独立赛事详情页 (P5) Upcoming 状态不显示报名开放/截止时间，仅显示比赛时间
> - 独立赛事详情页 (P5) 重要时间节点标签改为 i18n 翻译（报名开放/报名截止/比赛时间）
> - 排行榜页 (P8) 原生 `<select>` 替换为自定义 Dropdown 组件，游戏筛选"全部"改为"全部游戏"
> - **首页 (P3) 近期赛事**改为仅展示可报名的独立赛事和有可报名子赛事的锦标赛，按报名开放时间由新到旧排序
> - **赛事列表 (P4) 重构**：移除全部筛选和排序选项，改为"报名中"和"已结束赛事"两个区域，按报名开放时间/比赛时间排序
> - 服务器信息卡片改为报名后始终显示，管理员未填写时提示"比赛进入方式稍后提供"
> - 首页 banner 简化为单一"查看全部赛事"按钮
> - 赛事卡片（独立赛事/锦标赛）统一布局：名称移至内容区、状态标签统一在底行右对齐、锦标赛卡片新增下一场赛道/开赛时间/报名人数
> - 独立赛事详情页 (P5) 移除"报名开放时间"和"发布即开放"字段，仅保留报名截止时间和比赛时间
> - 锦标赛详情页 (P6) 侧边栏 sticky 固定范围扩大至服务器信息卡片
> - 我的赛事 (P12) 修正 Tab 过滤：锦标赛子赛事按各自状态分别归入对应 tab，不再整组跟随
> - 我的赛事 (P12) 独立赛事条目新增 Zap 图标和游戏平台标签，与锦标赛条目对齐
> - Mock 数据修正：Upcoming 状态赛事移除 d1 注册记录（报名未开放不应有注册）
> - 页面总数从 20 调整为 19（原锦标赛列表页与首页锦标赛专区合并至赛事列表）
> - 导航栏移除"锦标赛"独立入口，锦标赛通过赛事列表页筛选访问
> - v2.1 — 游戏平台新增 rF2、ETS2，移除 AMS2；赛事详情页服务器信息根据游戏类型（A类自建/B类官方）展示不同内容及加入指引
> - v2.2 — `access_requirements` 改为双语字段（`accessRequirements_zh` / `accessRequirements_en`），同步更新赛事详情页和锦标赛详情页的展示与报名逻辑
> - v2.3 — MOZA 设备展示改为从 Pit House 自动同步已拥有设备，用户仅控制推送渠道；我的赛事 (P12) Tab 合并为"已报名"+"已完成"，已完成不显示状态标签和日历按钮
> - v2.4 — PRD 明确多区域赛事规则：区域仅作为发现和筛选维度，不创建独立赛事实例；报名数据全局共享，同一赛事无论从哪个区域浏览均为同一条报名记录
> - v2.5 — 明确区域筛选总原则：公共内容（赛事/锦标赛/新闻/日历/排行榜）按区域筛选，用户个人数据（我的赛事/通知/抗议/个人统计）不按区域筛选；排行榜仅统计当前区域赛事成绩和积分，切换区域后重新计算；跨区域报名提示文案更新为服务器连通性提示
> - v3.0 — 原型层面改为统一赛事模型：P4 展示 Competition 卡片，P5 为统一赛事详情页，P6 为分站详情页；赛事结构改为 Competition → Round → Stage → Session，支持每个 Round 内含预选赛、正赛日、决赛等 Stage；抗议、成绩、服务器信息绑定到具体 Session
> - v3.1 — MVP 阶段只支持 AC / ACC；P5/P6 服务器信息展示改为来自后台自动开服配置的可见子集，新增不展示端口、管理员密码、Steam ID/GUID、插件地址等后台字段的规则
